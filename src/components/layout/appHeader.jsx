import { UI_LABELS } from '../../constants';

const AppHeader = () => {
  return (
    <header className="app-header">
      <h1>{UI_LABELS.APP_TITLE}</h1>
    </header>
  );
};

export default AppHeader;
